# Optional. Without these values, the project uses an in-memory preview store.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Set this to the number of signups that existed before this deployment.
WAITLIST_INITIAL_COUNT=8
